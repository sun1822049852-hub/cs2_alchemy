# UI 学习总结（当前项目实践版）

## 1. “描述词”是什么

在这个项目里你看到的“描述词”，通常指 Skill 文件头部的 `description` 字段。  
它的作用不是写实现细节，而是告诉系统：

- 这个 Skill 在什么场景下应该被触发
- 解决什么类型的问题

一句话：`description` = 触发条件说明，不是实现方案说明。

## 2. 当前 UI 架构结论

这套 UI 逐步收敛到了比较稳的分层方式：

- UI 层：只做展示和交互，不直接写库存业务逻辑
- 服务层：封装刷新、组件存取、炼金等接口
- 运行时/会话层：管理连接状态、会话复用、定时刷新

核心原则：UI 不“猜状态”，只消费接口结果和事件。

## 3. 状态同步的关键做法

- 同一份状态源驱动多个页面（库存页 + 炼金页）
- 顶部连接状态、库存时间、账号选择、刷新按钮统一同步
- 页面切换时只触发渲染，不重建业务状态

## 4. 事件驱动的实践

- 后端刷新完成后推送事件（SSE）
- 前端收到事件后更新数据和状态显示
- 自动刷新由后端调度，前端只负责展示结果

这比前端轮询更稳，职责更清晰。

## 5. 可复用的设置抽屉模式

已沉淀为全局 Skill：  
`C:/Users/18220/.agents/skills/local/ui-settings-drawer-pattern/SKILL.md`

关键约束：

- 文案在前、复选框在后
- 抽屉默认隐藏，可开可关
- `.hidden` 不能被组件样式覆盖
- 防全局 `input` 样式污染 checkbox
- 窄屏不出现逐字竖排

## 6. 典型坑位与修复经验

1. 复选框变大方框  
- 原因：全局 `input` 最小宽度污染  
- 修复：单独覆盖 `input[type="checkbox"]`

2. 齿轮打开后关不掉  
- 原因：`.hidden` 被 `display:flex` 覆盖  
- 修复：补充 `.settings-panel.hidden { display: none; }`

3. 布局拖拽中间有明显“长条”  
- 方案：改为边缘透明热区拖拽，不保留可见分隔条

4. 文本挤成竖排  
- 方案：设置弹层最小宽度 + 行内 `white-space: nowrap`

## 7. UI 规范建议（后续继续执行）

- 新功能先定层次：UI/服务/运行时职责先划清，再写代码
- 页面状态统一入口，不要多处维护“同义状态”
- 用户可见文案和日志文案分层
- 所有中文文件统一 UTF-8，避免编码回归
- 每次大改前做 checkpoint 提交，保证可回退

## 8. 下一阶段可继续优化

1. 把常见 UI 组件（抽屉、任务浮窗、筛选菜单）模块化  
2. 给关键交互补 UI 自动化冒烟用例  
3. 固化“样式验收清单”（对齐、可关闭、窄屏、禁用态、错误态）

