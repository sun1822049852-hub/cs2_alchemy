# 卡磨损的数学模型实现

## 这份文档是干什么的

这份文档把“卡磨损”从业务术语中剥离出来，抽象成一个纯数学问题，方便直接口述给 AI：

- 它不是游戏服务端 float 公式
- 它不是连续优化问题
- 它不是简单 greedy
- 它是一个有限离散集合上的单侧均值逼近优化问题

更准确地说，它解决的是：

> 从一个或多个有限候选集合中，选出固定数量的元素，使整体均值尽量逼近目标值，但必须严格低于目标值，并按多目标字典序做稳定优化。

---

## 三层数学模型

### 1. 通用母模型

给定若干候选集合：

- `S1, S2, ..., Sm`
- 每个 `Si` 都是有限离散集合
- 每个元素是一个实数，记作 `x`

从每个集合中选出固定数量：

- `Xi ⊆ Si`
- `|Xi| = ki`

总选择数：

- `K = k1 + k2 + ... + km`

整体均值定义为：

```text
μ(X) = (1 / K) * Σ_i Σ_{x∈Xi} x
```

给定目标值：

- `t`

给定安全边界：

- `ε > 0`

可行解必须满足：

```text
μ(X) < t - ε
```

目标不是最小化单一误差，而是：

```text
min score(X)
```

其中 `score(X)` 是一个多维 tuple，按字典序比较。

---

### 2. 单数组模型

只有一个集合：

```text
S = {x1, x2, ..., xn}
```

从中选 `k` 个元素：

```text
X ⊆ S, |X| = k
```

均值：

```text
μ(X) = (1 / k) * Σ_{x∈X} x
```

约束：

```text
μ(X) < t - ε
```

可以定义：

- `radius(X) = max_{x∈X} |x - t|`
- `aboveCount(X) = count(x > t)`
- `belowCount(X) = count(x < t)`
- `balance(X) = |aboveCount(X) - belowCount(X)|`
- `var(X) = variance(X)`
- `mad(X) = mean(|x - t|)`

于是单数组模型的字典序目标可写成：

```text
min lex(
  t - μ(X),
  radius(X),
  balance(X),
  var(X),
  mad(X)
)
```

含义：

- 第一优先级：均值尽量贴近目标，但必须在下方
- 第二优先级：选中值不要离目标太散
- 第三优先级：尽量兼顾目标两侧的分布平衡
- 第四、第五优先级：进一步稳定解

---

### 3. 多数组模型

给定多个集合：

```text
S1, S2, ..., Sm
```

每组固定选取：

```text
Xi ⊆ Si, |Xi| = ki
```

整体解：

```text
X = X1 ∪ X2 ∪ ... ∪ Xm
```

整体均值：

```text
μ(X) = (1 / K) * Σ_i Σ_{x∈Xi} x
```

约束：

```text
μ(X) < t - ε
```

定义：

- `radius(X) = max |x - t|`
- `var(X) = variance(all selected values)`
- `mad(X) = mean(|x - t|)`

若没有角色偏置，则字典序目标可以写成：

```text
min lex(
  t - μ(X),
  radius(X),
  var(X),
  mad(X)
)
```

---

### 4. 角色偏置模型

对每个集合额外给定角色：

- `role_i ∈ {main, aux}`

希望：

- `main` 中被选中的值尽量在 `t` 上方
- `aux` 中被选中的值尽量在 `t` 下方

定义：

- `mainMean = mean(all selected main values)`
- `auxMean = mean(all selected aux values)`
- `wrongSidePenalty = count(main < t) + count(aux > t)`
- `radius(X) = max |x - t|`

约束不变：

```text
μ(X) < t - ε
```

则角色偏置模型的字典序目标为：

```text
min lex(
  t - μ(X),
  wrongSidePenalty,
  -mainMean,
  auxMean,
  radius(X)
)
```

含义：

- 第一优先级：整体均值从下方贴近目标
- 第二优先级：尽量减少站错边的元素
- 第三优先级：让 `main` 更高
- 第四优先级：让 `aux` 更低
- 第五优先级：压缩整体分布半径

---

## 给 AI 的数学版 Prompt

```text
你现在要从零实现一个“有限离散集合上的单侧均值逼近优化器”。

先校准边界：
1. 这不是任何游戏或业务领域专属公式。
2. 这不是连续优化问题。
3. 这不是无限逼近的实数模型。
4. 这是一个“从有限候选集合中选出固定数量元素，使整体均值尽量逼近目标值、但必须严格低于目标值”的离散组合优化问题。
5. 你必须按我下面给出的数学规格实现，不要擅自简化成普通 greedy。

一、统一母模型

给定若干候选集合：
- S1, S2, ..., Sm
- 每个 Si 都是有限离散集合
- 每个元素都是一个实数值，记作 x

从每个集合 Si 中选出固定数量 ki 个元素，得到 Xi：
- Xi ⊆ Si
- |Xi| = ki

总选择数：
- K = k1 + k2 + ... + km

定义整体均值：
- μ(X) = (1 / K) * Σ_i Σ_{x∈Xi} x

给定目标值：
- t

给定安全边界：
- ε > 0

可行解必须满足：
- μ(X) < t - ε

目标不是单纯最小误差，而是：
- 在所有可行解中做字典序最优化
- 即最小化一个 score tuple，而不是一个标量

二、三层模型

你需要同时支持这三层抽象：

1. 单数组模型
- 只有一个集合 S
- 从 S 中选 k 个元素
- 目标是在 μ(X) < t - ε 前提下，让均值尽量逼近 t

2. 多数组模型
- 有多个集合 S1...Sm
- 从每个集合中选固定数量 ki
- 目标仍是在 μ(X) < t - ε 前提下，让整体均值尽量逼近 t

3. 角色偏置模型
- 多个集合中，每个集合带角色 role
- role ∈ {main, aux}
- 希望：
  - main 组选中的值尽量高于 t
  - aux 组选中的值尽量低于 t
- 但整体均值仍必须严格低于 t - ε

三、不要误解成连续模型

必须明确遵守：
1. 每个集合是有限离散集合，不是区间。
2. 默认不放回选择，即同一元素不能重复选两次。
3. 因为是有限离散集合，所以通常不会真的“无限逼近”目标值。
4. 更准确的定义是：
   - 从有限可行解集合中，找出最接近目标下界的最优解
5. 只有当我额外声明“允许重复取值”或“集合近似连续”时，才允许把它理解成可无限逼近模型。

四、单数组模型的数学目标

给定：
- S = {x1, x2, ..., xn}
- 选 k 个元素，得到 X

约束：
- |X| = k
- μ(X) < t - ε

定义：
- μ(X) = mean(X)
- radius(X) = max_{x∈X} |x - t|
- aboveCount(X) = 选中后严格大于 t 的元素个数
- belowCount(X) = 选中后严格小于 t 的元素个数
- balance(X) = |aboveCount(X) - belowCount(X)|
- var(X) = variance(X)
- mad(X) = mean(|x - t|)

单数组模型采用字典序目标：
- min lex(
    t - μ(X),
    radius(X),
    balance(X),
    var(X),
    mad(X)
  )

解释：
- 第一目标：均值尽量贴近 target，但必须在下方
- 第二目标：选中元素不要离 target 太散
- 第三目标：尽量兼顾 target 两侧分布平衡
- 第四、第五目标：进一步减少离散度

五、多数组模型的数学目标

给定：
- S1...Sm
- 每组选择 ki 个元素
- Xi ⊆ Si
- |Xi| = ki

定义：
- X = X1 ∪ X2 ∪ ... ∪ Xm
- μ(X) = 所有被选元素的总平均值

约束：
- 对每个 i，|Xi| = ki
- μ(X) < t - ε

定义：
- radius(X) = max |x - t|
- var(X) = variance(all selected values)
- mad(X) = mean(|x - t|)

多数组无角色偏置模型采用字典序目标：
- min lex(
    t - μ(X),
    radius(X),
    var(X),
    mad(X)
  )

六、角色偏置模型的数学目标

对于每个集合 Si，额外给一个角色：
- role_i ∈ {main, aux}

定义：
- mainMean = 所有 main 组选中值的平均值
- auxMean = 所有 aux 组选中值的平均值
- wrongSidePenalty =
    main 中小于 t 的元素数量
    + aux 中大于 t 的元素数量
- radius(X) = max |x - t|

约束不变：
- 每组仍选固定数量
- 整体均值仍满足 μ(X) < t - ε

角色偏置模型采用字典序目标：
- min lex(
    t - μ(X),
    wrongSidePenalty,
    -mainMean,
    auxMean,
    radius(X)
  )

解释：
- 第一目标：整体均值尽量从下方逼近 target
- 第二目标：减少站错边的元素
- 第三目标：让 main 更高
- 第四目标：让 aux 更低
- 第五目标：最后再压缩整体半径

七、实现要求

你必须实现一个可运行 solver，而不是只写数学说明。

至少输出：
1. 一个高层求解入口
   - 输入多个数组 / 分组 / 角色 / target / epsilon / 每组选取数量
   - 输出最优选择结果
2. 一个 score tuple 计算函数
3. 一个 compareScoreTuples 函数
   - 必须按字典序逐位比较
4. 一个 replay 能力
   - 给定固定输入，能打印：
     - selected items
     - overall mean
     - score tuple
     - 关键搜索过程 trace
5. 完整测试

八、搜索要求

不要写成一句话贪心。

你必须把它当作离散组合优化来实现，并根据模型复杂度选择合理搜索：
1. 单数组模型：
   - 允许用窗口推进、局部交换、补偿修正
   - 但不能退化成只取“最接近 target 的前 k 个”
2. 多数组模型：
   - 允许用 beam search、分层搜索、剪枝
   - 但不能写成随机搜索
3. 角色偏置模型：
   - 必须保留角色约束
   - 不能把 role 去掉后当普通多数组处理

九、deterministic 要求

必须 deterministic：
1. 相同输入必须得到相同输出
2. 所有 tie-break 都必须显式定义
3. 不能依赖哈希遍历顺序
4. 当 score tuple 完全相同，使用稳定次序继续比较：
   - 先比较均值
   - 再比较元素值序列
   - 再比较元素 id / index

十、测试要求

至少覆盖以下测试：
1. 单数组：
   - 能找到严格低于 target 的最优均值
2. 单数组平衡性：
   - 不是简单拿一边的最近值
3. 多数组：
   - 每组数量约束严格生效
4. 角色偏置：
   - main 更偏向 target 上方
   - aux 更偏向 target 下方
5. 不可行情况：
   - 若不存在 μ(X) < t - ε 的解，必须明确返回失败
6. deterministic：
   - 相同输入多次运行结果一致

十一、输出顺序

请按以下顺序输出：
1. 用不超过 12 句总结你理解的数学模型
2. 给出完整可运行代码
3. 给出完整测试
4. 给出 replay 示例
5. 给出测试结果摘要
6. 给出“你的实现如何对应单数组 / 多数组 / 角色偏置三层模型”的核对表

最后强调：
- 这不是业务术语题
- 这不是游戏公式题
- 这是有限离散集合上的单侧均值逼近优化问题
- 不要偷换成连续优化
- 不要偷换成简单贪心
- 不要只优化一个误差值
- 必须保留字典序 score tuple
```

---

## 超短口述版

如果你想更短一点口述给 AI，可以直接说：

```text
你现在实现一个有限离散集合上的单侧均值逼近优化器。输入是一组或多组数组，每组要选固定数量元素，目标是让整体均值尽量逼近 target，但必须严格满足 mean < target - epsilon。不要写成 greedy，要写成离散搜索。单数组模型优化 lex(target-mean, radius, balance, variance, mad)；多数组模型优化 lex(target-mean, radius, variance, mad)；带角色模型优化 lex(target-mean, wrongSidePenalty, -mainMean, auxMean, radius)。要求 deterministic、可 replay、带测试、带 score tuple 比较函数。
```

---

## 这套抽象最容易被 AI 说歪的地方

### 错误 1：把它说成连续优化

错误说法：

> 给定区间后连续调参，让均值无限逼近目标。

正确说法：

> 从有限离散候选中选固定数量元素，寻找最优可行解。

### 错误 2：把“均值低于目标”说成“每个元素都低于目标”

错误说法：

> 所有被选值都要小于 target。

正确说法：

> 允许部分元素高于 target，只要整体均值仍低于 target 即可。

### 错误 3：只优化一个误差值

错误说法：

> 只要最小化 `target - mean` 就够了。

正确说法：

> 需要字典序多目标优化，否则会丢掉平衡性、角色偏置和稳定性。

### 错误 4：把角色模型退化成普通多数组

错误说法：

> main / aux 只是标签，不影响求解。

正确说法：

> 角色偏置模型必须显式优化 `wrongSidePenalty`、`mainMean`、`auxMean`。

---

## 使用后的最低验收标准

至少检查：

1. AI 是否明确说出“有限离散集合上的单侧均值逼近优化问题”。
2. 是否明确区分单数组、多数组、角色偏置三层模型。
3. 是否保留了 `mean < target - epsilon` 这个硬约束。
4. 是否真的实现了 `score tuple` 和 `compareScoreTuples`。
5. 是否给出了 deterministic 的 tie-break。
6. 是否真的输出了代码、测试、replay，而不是只写数学定义。

