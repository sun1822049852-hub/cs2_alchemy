# Workspace Workflow

- 默认直接在主工作区 `C:\Users\18220\Desktop\cs2_alchemy` 修改，不使用 Git worktree。
- 只有用户明确要求隔离开发、并行分支或风险隔离时，才允许创建和使用 worktree。
- 默认使用主工作区启动和调试，这样可以直接复用主目录下的账号、token、UI 状态和数据库。
- `csgo_skins.db`、`inventory_ui_state.json`、`backup/ui_state/` 下的快照文件属于运行态产物；只要本地 Electron / sidecar 正在运行，这些文件在 `git status` 中持续变脏是正常现象，不能单独据此判定本次改动异常。
- 做工作区检查、提交前核对或 diff 分析时，若发现上述运行态文件仍在变化，先确认是否只是本地程序仍在写入；除非用户明确要求“全部提交”或本轮任务本身就涉及这些文件，否则默认不把它们当作需要处理的业务改动。
- 除非用户明确要求提交，否则修改保持未提交状态，先让用户在主程序里验证。
- 用户确认“不符合”时，不提交；按用户要求回滚本次由 agent 产生的改动。
