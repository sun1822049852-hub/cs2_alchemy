# Workspace Workflow

- 默认直接在主工作区 `C:\Users\18220\Desktop\cs2_alchemy` 修改，不使用 Git worktree。
- 只有用户明确要求隔离开发、并行分支或风险隔离时，才允许创建和使用 worktree。
- 默认使用主工作区启动和调试，这样可以直接复用主目录下的账号、token、UI 状态和数据库。
- `csgo_skins.db`、`inventory_ui_state.json`、`backup/ui_state/` 下的快照文件属于运行态产物；只要本地 Electron / sidecar 正在运行，这些文件在 `git status` 中持续变脏是正常现象，不能单独据此判定本次改动异常。
- 做工作区检查、提交前核对或 diff 分析时，若发现上述运行态文件仍在变化，先确认是否只是本地程序仍在写入；除非用户明确要求“全部提交”或本轮任务本身就涉及这些文件，否则默认不把它们当作需要处理的业务改动。
- 除非用户明确要求提交，否则修改保持未提交状态，先让用户在主程序里验证。
- 用户确认“不符合”时，不提交；按用户要求回滚本次由 agent 产生的改动。


## Sandbox / 提权铁律

- 对于测试、构建、启动、编译、watch、dev server、集成验证类命令，只要高概率会派生子进程、写临时文件或 runtime 目录、绑定本地端口、访问受限目录或工作区外目录，则禁止先在 sandbox 内试跑。
- 上述命令首次执行即直接使用 escalated 权限，并同时申请可复用的 prefix rule；不要通过先失败一次来探测 sandbox 边界。
- 在本项目中，`node --test`、`npm test`、`npm start`、`javac`、`java`、以及其他会派生子进程的验证或启动命令，默认按上述规则处理。
- 仅当命令属于纯只读检查，且已知在 sandbox 内稳定成功时，才允许不提权直接执行。

## UI / Frontend 调试偏好

- 当任务涉及前端、UI、交互、页面重构、视觉微调、响应式、缩放、overflow、详情页适配时，先读取并遵守 `skills/ui-runtime-guardrails/SKILL.md`。
- 当任务涉及前端、UI、交互、页面重构、视觉微调时，默认优先使用**真实运行态联调**：直接修改真实项目代码、启动本地应用、让用户在浏览器或程序界面中查看真实 UI 效果。
- 默认不要使用 `companion`、mock 页面、原型页、静态演示页来替代真实项目实现，除非用户明确要求“先样例后实现”或明确要求使用 `companion`。
- 如果用户已经表达“继续用当前这种调试模式”“直接在浏览器看真实 UI”“不要 companion”，则后续同类任务都按真实运行态联调处理，不再重复切回原型演示路线。
- 对 UI 调整，优先让用户看真实运行结果再继续迭代；不要把“看起来接近”的原型稿当作已经实现。
- 未经过浏览器 / 程序运行态验证与运行态证据抓取时，不得把源码测试、静态断言、字符串命中当作“已经生效”的依据。





