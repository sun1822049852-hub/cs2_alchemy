# 新登录方式说明

## 改进内容

### 之前的问题
- 使用自定义的 HTTPS API + access_token 方式
- 没有使用 Steam 的 sentry 机制（设备认证）
- 每次登录都需要重新走完整流程
- Steam CM 服务器检测到异常行为，主动断开连接

### 现在的方案
- **使用 `steam[client]` 库的标准登录方式**
- **完全模拟 Steam 官方客户端的行为**
- **支持 login_key 和 sentry 持久化**
- **登录一次后长期有效**

---

## 工作原理

### 首次登录
1. 用户输入：账号密码 + 手机令牌（5位数字）
2. 程序调用：`client.login(username, password, two_factor_code=totp)`
3. Steam 服务器返回：
   - `login_key`（类似 refresh_token，长期有效）
   - `sentry file`（设备认证文件，证明这是受信任的设备）
4. 程序自动保存：
   - `login_key` → `login_keys.json`
   - `sentry file` → `sentries/sentry_<username>.bin`

### 后续登录
1. 程序读取：`login_key` 和 `sentry file`
2. 程序调用：`client.login(username, login_key=saved_key)`
3. Steam 服务器识别：
   - 有 `sentry file` → 这是受信任的设备
   - 有 `login_key` → 无需密码和 2FA
4. **自动登录成功，无需任何输入**

---

## 使用方法

### 测试新登录流程

```bash
# 运行测试脚本
.venv\Scripts\python test_login.py

# 首次登录：输入账号密码 + 手机令牌
# 再次运行：自动登录，无需输入
```

### 在主程序中使用

主程序 `main.py` 和 `account_manager.py` **无需修改**，因为：
- `login()` 函数签名保持不变
- `login_with_totp()` 函数签名保持不变
- 只是内部实现改为使用标准方式

---

## 文件说明

### 新增文件
- `login_keys.json` - 保存所有账号的 login_key
- `sentries/sentry_<username>.bin` - 每个账号的 sentry 文件

### 修改文件
- `auth.py` - 完全重写，使用 `steam[client]` 标准方式

### 删除文件（可选）
- `login_key_cache.json` - 旧的缓存文件，可以删除

---

## 优势

1. **完全模拟官方客户端**：Steam 服务器无法区分
2. **登录一次长期有效**：就像 Steam 客户端一样
3. **代码更简洁**：删除了 300+ 行自定义代码
4. **更稳定可靠**：使用官方库的标准实现
5. **支持账号切换**：每个账号独立的 login_key 和 sentry

---

## 常见问题

### Q: login_key 会过期吗？
A: 通常不会，除非：
- 在其他设备上修改了密码
- 手动撤销了设备授权
- Steam 检测到异常行为

### Q: 如果 login_key 过期怎么办？
A: 程序会自动检测并提示重新初始化登录（输入密码 + 令牌）

### Q: sentry 文件是什么？
A: 设备认证文件，证明这台电脑是受信任的设备，类似于"记住此设备"功能

### Q: 可以在多台电脑上使用吗？
A: 可以，但每台电脑需要独立初始化登录（生成各自的 sentry 文件）

---

## 与旧版本的兼容性

- ✅ `login(username, password)` - 保持兼容
- ✅ `login_with_totp(username, password, totp)` - 保持兼容
- ✅ `account_manager.py` - 无需修改
- ✅ `main.py` - 无需修改

**旧的 `login_key_cache.json` 不兼容，需要重新初始化登录。**
