# CS2 炼金工具 - 使用指南

## 快速开始

### 1. 运行程序

**方法一：双击运行**
```
双击 run.bat 文件
```

**方法二：命令行运行**
```bash
# 激活虚拟环境
.venv\Scripts\activate

# 运行主程序
python main.py
```

### 2. 首次使用 - 添加账号

1. 启动程序后，选择 `1. 账号管理`
2. 选择 `2. 新增账号`
3. 输入 Steam 用户名
4. 输入密码
5. **准备好手机令牌 App**（Steam Guard Mobile Authenticator）
6. 输入当前的 5 位验证码
7. 等待初始化完成

**重要提示**：
- 验证码有时效性（30秒），如果超时请重新输入
- 初始化成功后，refresh_token 会保存到 `login_key_cache.json`
- 后续登录无需再输入密码和验证码

### 3. 进入炼金程序

1. 主菜单选择 `2. 进入炼金程序`
2. 程序会自动：
   - 连接 Steam CM 服务器（WebSocket）
   - 登录 Steam 账号
   - 连接 CS2 GC（Game Coordinator）
   - 等待 SO Cache 推送（库存数据）
3. 显示完整库存信息

## 主菜单说明

```
====================================
  当前账号：[账号名或"未选择"]
====================================
  1. 账号管理（新增 / 切换 / 删除）
  2. 进入炼金程序
  0. 退出
====================================
```

### 账号管理子菜单

```
=== 账号管理 ===
当前账号：[账号名]
  1. 切换账号    - 在已保存的账号间切换
  2. 新增账号    - 添加新的 Steam 账号
  3. 删除账号    - 删除已保存的账号
  0. 返回主菜单
```

## 常见问题

### Q1: 提示 "ModuleNotFoundError: No module named 'gevent'"

**解决方法**：
```bash
# 确保使用虚拟环境的 Python
.venv\Scripts\python main.py

# 或者先激活虚拟环境
.venv\Scripts\activate
python main.py
```

### Q2: 验证码输入后提示 "登录失败"

**可能原因**：
1. 验证码超时（30秒有效期）
2. 网络连接问题
3. Steam 服务器繁忙

**解决方法**：
- 重新输入最新的验证码
- 检查网络连接
- 稍后重试

### Q3: 提示 "无法连接到 Steam CM 服务器"

**可能原因**：
1. 网络防火墙阻止 443 端口
2. Steam CM 服务器维护
3. 本地网络问题

**解决方法**：
- 检查防火墙设置，允许程序访问网络
- 尝试使用 VPN 或代理
- 稍后重试

### Q4: 提示 "CS2 GC 连接超时"

**可能原因**：
1. CS2 GC 服务器繁忙
2. 账号没有 CS2 许可
3. 网络不稳定

**解决方法**：
- 确认账号拥有 CS2 游戏
- 检查网络稳定性
- 重新运行程序

### Q5: 中文显示乱码

**原因**：Windows 控制台默认使用 GBK 编码

**解决方法**：
- 使用 `run.bat` 启动（已自动设置 UTF-8）
- 或手动执行：`chcp 65001` 后再运行程序

## 文件说明

### 配置文件

- `accounts.json` - 账号列表和当前激活账号
- `login_key_cache.json` - refresh_token 缓存（**重要：请勿泄露**）
- `machine_id.bin` - 机器 ID（用于 Steam 登录）
- `schema_cache.json` - 物品 Schema 缓存

### 日志输出

程序使用 Python logging 模块输出调试信息：
- `DEBUG` 级别：详细的协议交互信息
- `INFO` 级别：关键操作信息
- `WARNING` 级别：警告和错误信息

## 安全提示

1. **保护 refresh_token**
   - `login_key_cache.json` 包含登录凭证
   - 不要分享给他人
   - 定期更换密码

2. **账号安全**
   - 建议使用小号测试
   - 不要在公共网络使用
   - 注意 Steam 账号安全规则

3. **使用限制**
   - 遵守 Steam 服务条款
   - 不要频繁操作（避免触发限流）
   - 仅用于学习和研究

## 技术支持

### 模块测试

运行模块导入测试，检查所有依赖是否正常：
```bash
.venv\Scripts\python test_imports.py
```

### 查看日志

程序运行时会输出详细的调试信息，包括：
- Steam CM 连接状态
- GC 消息交互
- 账号限制状态
- 库存数据解析

### 依赖版本

查看已安装的包版本：
```bash
.venv\Scripts\pip list
```

## 下一步开发

当前版本已实现：
- ✅ 多账号管理
- ✅ 免密登录
- ✅ WebSocket CM 连接
- ✅ CS2 GC 连接
- ✅ 库存读取
- ✅ 炼金协议框架

待实现功能：
- ⏳ 物品筛选逻辑（按稀有度、品质、磨损值）
- ⏳ 配方 ID 映射（quality + rarity → recipe_id）
- ⏳ 自动炼金执行
- ⏳ 炼金结果统计

## 参考资料

- [Steam 协议文档](https://github.com/SteamDatabase/Protobufs)
- [ValvePython/steam](https://github.com/ValvePython/steam)
- [ValvePython/csgo](https://github.com/ValvePython/csgo)
- [CS2 物品数据 API](https://github.com/ByMykel/CSGO-API)
