# 项目开发规范（可执行版）

适用范围：`cs2_alchemy` 当前 Node + Python（Legacy）混合项目。  
目标：降低耦合、减少回归、让多人协作行为一致。

## 1. 架构总原则

### 1.1 分层职责（必须）
- `UI层`：只做展示、交互、页面状态管理。
- `接口层`：接收 UI 请求，编排业务流程，返回结构化结果。
- `领域/数据层`：登录、库存拉取、协议解析、快照读写、缓存策略。

### 1.2 依赖方向（必须）
- 只允许 `UI -> 接口 -> 领域/数据` 单向依赖。
- 禁止 `UI` 直接调用 Steam/GC、直接读写底层快照文件、直接拼底层协议对象。

### 1.3 当前项目映射
- `node_sidecar/ui/*`：UI层
- `node_sidecar/src/uiServer.js`：接口层入口
- `node_sidecar/src/*.js`（除 `uiServer.js`）：领域/数据层
- `main_ui.py/main.py`：Legacy（仅回退）

### 1.4 时序与调度归属（强制）
- 凡是“何时触发业务动作”的策略（如自动刷新、重试、超时重连、心跳）必须在后端实现。
- UI 层禁止实现业务调度器（例如 `setInterval` 定时触发 `/api/refresh`）。
- UI 只允许做“显示同步”：接收后端事件后更新页面，或拉取已完成结果。
- 推送优先级：`SSE/WebSocket` > 轮询。轮询仅允许作为推送不可用时的兜底。
- 兜底轮询必须标记 `TEMP_HACK`，并登记回收计划（参见 4.2 / 4.3）。

---

## 2. UI 层硬约束

### 2.1 允许做的事
- 页面渲染、按钮事件、输入校验（格式级）。
- 视图相关处理：筛选、排序、分页、分组、展开态。
- 通过 `fetch('/api/...')` 调接口并展示结果。

### 2.2 禁止做的事
- 禁止在 UI 层实现登录协议、库存解析、组件解析。
- 禁止 UI 直接访问数据库/本地快照文件。
- 禁止 UI 层出现 Steam/GC SDK 调用。
- 禁止 UI 维护业务倒计时并据此触发后端业务动作（如“30分钟到点自动刷新库存”）。

### 2.3 接口调用规范
- UI 只调用 `/api/*`。
- 每次调用必须有失败分支（`try/catch` + 用户可见提示）。
- 对高频操作加防抖/节流（例如搜索 150~250ms，刷新最小间隔 10s）。

---

## 3. 接口层规范

### 3.1 返回结构（建议统一）
- 成功：`{ ok: true, ... }`
- 失败：`{ ok: false, message: "可读错误" }`

### 3.2 接口职责
- 接口层只编排，不写复杂算法。
- 复杂解析逻辑下沉到 `src` 领域模块。

### 3.3 状态一致性
- 账号切换、刷新完成后，必须同步 UI 所需状态（如 `last_selected_username`、`fetch_time`）。

---

## 4. 严禁无登记折中（新增）

### 4.1 禁止事项（强制）
- 禁止“先把业务逻辑放到 UI，后面再改”的无登记折中实现进入主线。
- 禁止跨层写法（例如前端写后端调度策略、后端写界面状态逻辑）直接合并。

### 4.2 允许例外的唯一条件
必须同时满足以下 5 项，才允许临时折中：
1. 建立一条 `临时折中单`（Issue 或文档编号）。
2. 写明：`原因`、`影响范围`、`风险`、`回收方案`。
3. 指定：`负责人` 和 `截止日期`（最长 7 天）。
4. 在代码处加注释：`TEMP_HACK(<编号>, 截止 YYYY-MM-DD)`。
5. PR 描述中明确写出“这是临时方案”。

### 4.3 到期处理（强制）
- 到期未回收：禁止继续叠加功能，优先还债。
- 新功能依赖旧临时方案时，必须先回收旧方案再开发。

---

## 5. 代码风格规范

### 5.1 通用
- 函数单一职责：一个函数做一件事。
- 避免超长函数（建议 < 80 行），超出就拆分。
- 禁止“单行压缩式”代码提交（可读性优先）。
- 注释只写“为什么”，少写“做了什么”。

### 5.2 命名
- 变量/函数：语义化英文小驼峰（JS）或蛇形（Python）。
- 布尔值以 `is/has/can/should` 开头。
- 临时变量禁用 `a/b/tmp1` 这类无语义命名。

### 5.3 文件职责
- 一个文件只承载一个明确领域。
- UI 文件不要混入协议常量和底层对象定义。

---

## 6. 错误处理与日志

### 6.1 错误处理
- 禁止吞异常（`catch {}`）而不记录。
- 用户可恢复错误必须给清晰中文提示（例如“请检查代理后重试”）。

### 6.2 日志
- 日志分级：`DEBUG/INFO/WARN/ERROR`。
- 接口层记录关键节点：开始、成功、失败、耗时、关键计数。
- 重复日志做去重，避免刷屏影响排查。

---

## 7. 性能规范（UI重点）

- 列表渲染必须分页。
- 搜索必须防抖。
- 重计算必须做缓存（例如筛选结果、分组结果、菜单选项）。
- 只在数据源变化时重建菜单和大块 DOM。

---

## 8. 测试与验收

### 8.1 最低验收项（每次改动）
- 能启动（语法检查通过）。
- 账号切换、刷新、筛选、分页无报错。
- 失败路径提示可读。

### 8.2 建议命令
- Node 语法：`node --check node_sidecar/ui/app.js`
- Python 语法：`python -m py_compile main_ui.py main.py`

---

## 9. 变更流程（建议）

1. 改动前说明目标与影响范围。  
2. 先改最小闭环，再扩展。  
3. 每次大改前做 checkpoint（可回滚）。  
4. 提交说明必须写清：做了什么、为什么、怎么验证。
5. 涉及“调度位置/通信模型”变更时，必须在说明中单列：
   - 调度归属（前端/后端）
   - 通知机制（SSE/WebSocket/轮询）
   - 兜底策略与回收时间

---

## 10. 提交前检查清单

- [ ] UI 是否仅通过 `/api/*` 交互？
- [ ] 业务解析是否都在后端/领域层？
- [ ] 自动刷新/重试/心跳等调度是否都在后端？
- [ ] 前端是否仅做显示同步（无业务定时器）？
- [ ] 是否有吞异常或无提示失败？
- [ ] 是否引入重复重算和重复请求？
- [ ] 是否保留回退路径（当前 Python Legacy）？
- [ ] 是否存在 `TEMP_HACK`？若存在，是否有编号和截止日期？

---

## 11. 一句话版本

**UI 只负责“看见和点击”，业务只走接口，协议与数据处理全部下沉到后端。**

**临时折中不是禁止，但必须“有单、有主、有限期、可回收”。**
