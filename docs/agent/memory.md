# Memory

- 2026-04-29: 后续“批量上架”能力不能完整复用当前库存总览页面的显示规则。原因是库存总览当前会展示黄盾物品；因此应复用其 **GC 数据、选中态、组件取出与部分展示能力**，但上架流需要单独的“可卖/不可卖”选择规则与大窗口交互。
- 2026-04-29: 当前前端里黄盾物品已经不可选；后续若把批量上架入口接到库存总览，不要重复发明一套黄盾前端拦截，但仍需单独确认冷却、`flags=24` 隐藏条目、以及 `Storage Unit` 本体在上架流中的禁用表现。
- 2026-04-29: 这台机器当前把浏览器流量走在 Windows Internet Settings 系统代理上，现场值为 `127.0.0.1:15732`；但 [node_sidecar/src/networkPrecheck.js](/C:/Users/18220/Desktop/cs2_alchemy/node_sidecar/src/networkPrecheck.js) 的 `getProxyUrl()` 只读取 `HTTPS_PROXY/HTTP_PROXY/ALL_PROXY` 环境变量，不会自动吃系统代理。因此凡是从 shell/Node 里直接拉 `steamcommunity.com`（尤其是真 Web inventory 对账），若不先显式设置 `HTTPS_PROXY`，就可能出现 `timeout / ECONNRESET`，即使浏览器/Electron 看起来已经“开了代理”。
- 2026-04-29: 对 `LOVE (countsteam002)` 的第一轮 live 对账已经证明：Web 主库存与 GC 主库存的重叠项应以 `assetid + market_hash_name` 作为主比较键，不应拿 `name` 直接比较。现场真 Web inventory 返回的是本地化中文 `name`，而 GC 快照里的 [inventory_processed_20260429_180408.json](/C:/Users/18220/Desktop/cs2_alchemy/logs/processed_inventory/inventory_processed_20260429_180408.json) 当前保留的是英文完整名；但两边重叠项的 `market_hash_name` 与 `assetid` 都能稳定对齐。
- 2026-04-29: [node_sidecar/ui/app.js](/C:/Users/18220/Desktop/cs2_alchemy/node_sidecar/ui/app.js) 里 `Web Inventory` 页当前“拉取库存”按钮主链已切回 [node_sidecar/src/uiServer.js](/C:/Users/18220/Desktop/cs2_alchemy/node_sidecar/src/uiServer.js) 的 `/api/accounts/:username/inventory`，底层通过账号 Web Session 去请求 `https://steamcommunity.com/inventory/{steamId64}/730/2?...`。因此这个按钮当前拿到的是 Community inventory 轻量字段，不应再把它当成 GC 快照读取入口；若后续还要看 `casket_id/float_value/paint_seed` 等 GC 明细，必须明确走 `/api/snapshot/account`。
- 2026-04-29: 对 `Moon set (wy19174601720)` 的最小真实 `withdraw` 样本已经证明：`asset_id` 是物品本身的稳定唯一标识；位置状态看 `casket_id`，在组件内时等于 Storage Unit 的 `asset_id`，回主库存后变成空字符串。后续凡是实现组件物品上架或做 `asset_id_reconcile_failed` 保险丝，都应以“重新定位到同一 `asset_id` 且 `casket_id` 已为空”作为继续执行的前提。
- 2026-04-29: 浏览器请求 `/api/snapshot/account` 返回的是完整 GC `rows + component`，不是轻量 Web inventory 卡片接口。原始响应里已带 `asset_id`、`casket_id`、`float_value`、`paint_seed`、`paint_index`、`tradable_after`、`hidden_reason`、`collection`、`goods_*_icon_url` 等详细字段；当前 [node_sidecar/ui/app.js](/C:/Users/18220/Desktop/cs2_alchemy/node_sidecar/ui/app.js) 只是把它适配成简化 `items` 供页面渲染，但原始明细仍保留在 `webInvState.inventoryCache.rows` 与 `items[*].raw_row` 中。
- 2026-04-29: 当前 `Web Inventory` 页面适配层会过滤掉 `Storage Unit` 本体（`def_index===1201`），但会保留组件内物品，并为其打上 `source_scope/source_component_id/source_component_name/is_component_item`。因此运行态网格中的卡片数应等于“原始 `row_count - storage_unit_count`”；只要组件预加载完整，页面就能同时看到 `市场出售 / 炼金材料1 / 炼金材料2` 等多个组件来源。
- 2026-04-25: [node_sidecar/src/tokenStore.js](/C:/Users/18220/Desktop/cs2_alchemy/node_sidecar/src/tokenStore.js) 只是基于 `login_keys.json` 的轻量 JSON store，没有 `close()`。后续凡是在 [node_sidecar/src/uiServer.js](/C:/Users/18220/Desktop/cs2_alchemy/node_sidecar/src/uiServer.js) 或其他运行态路径里 new `TokenStore()`，都不能无条件调用 `tokenStore.close()`；否则会在真实页面里把 `/api/accounts/enroll-steam-guard` 这类首步接口直接炸成 500。
- 2026-04-25: 前端 [node_sidecar/ui/app.js](/C:/Users/18220/Desktop/cs2_alchemy/node_sidecar/ui/app.js) 里的 `api()` helper 不仅返回解析后的 JSON，还会在 `HTTP 非 2xx` 或 `body.ok === false` 时直接抛错，并把业务载荷挂在 `err.data`。后续像 `Steam Guard enroll/finalize` 这种需要展示服务端业务错误的调用侧，必须优先读 `err.data.reason/message/status`，不能只在 `catch` 里拼 `请求失败：${err.message}`，否则真实 `ok:false` 会退化成 `请求失败：http 200`。
- 2026-04-25: 前端 [node_sidecar/ui/app.js](/C:/Users/18220/Desktop/cs2_alchemy/node_sidecar/ui/app.js) 里的 `api()` helper 已经返回解析后的 JSON，不是原始 `fetch Response`。后续凡是调用 `api(...)`，禁止再写 `const resp = await api(...); await resp.json()` 这种二次解析；这会在真实运行态里直接炸成 `resp.json is not a function`。本次已确认受影响的 `Steam Guard` 令牌链包括 enroll、finalize 与 token-detail。
- 2026-04-25: 魔尊提到的“昨天 cloud code 记忆”，按现场已映射并同步为仓库摘要 [docs/agent/claude-code-memory-sync-2026-04-24-to-2026-04-25.md](/C:/Users/18220/Desktop/cs2_alchemy/docs/agent/claude-code-memory-sync-2026-04-24-to-2026-04-25.md)。后续新会话优先读这份仓库摘要，而不是再默认回 `C:/Users/18220/.claude/projects/c--Users-18220-Desktop-cs2-alchemy/memory/` 逐个翻 `MEMORY.md`、`login-system-audit.md`、`sim-export-craft.md`、`craft-predictor-global.md`。
- 2026-04-25: Claude 在 `Web Market Listing` Phase 2 留下的冻结断点原件位于 [C:/Users/18220/.claude/projects/c--Users-18220-Desktop-cs2-alchemy/memory/market-listing-confirm.md](/C:/Users/18220/.claude/projects/c--Users-18220-Desktop-cs2-alchemy/memory/market-listing-confirm.md)，仓库内镜像位于 [docs/agent/market-listing-confirm.md](/C:/Users/18220/Desktop/cs2_alchemy/docs/agent/market-listing-confirm.md)。后续新会话优先读仓库镜像，不要再默认去 `.claude` 目录翻断点。
- 2026-04-25: `Web Inventory` 主线的上游来源已经核实：Claude 本地计划是 `C:/Users/18220/.claude/plans/purring-cooking-kernighan.md`，参考仓库是 `hjkkjh-hhh/steam-uu-c5-scratch-account_tool`。后续凡是要继续“顺着开源纠正实现”，先读 [docs/agent/web-inventory-upstream-implementation-index.md](/C:/Users/18220/Desktop/cs2_alchemy/docs/agent/web-inventory-upstream-implementation-index.md)，不要再从零翻 Claude 缓存。
- 2026-04-25: `Web Inventory` 的 `steamWebSession` 已完成第一刀对齐：token refresh 现在会带 `steamid`，检测到代理环境时会挂 `https-proxy-agent`，并优先走 `steam-session` `LoginSession(MobileApp).getWebCookies()`，失败时再退回手工 cookie，同时保留 `Steam_Language` / `timezoneOffset` / `browserid` / `steamCountry`。后续剩余的高风险项应按这个顺序继续处理：1) [node_sidecar/src/uiServer.js](/C:/Users/18220/Desktop/cs2_alchemy/node_sidecar/src/uiServer.js) 的 `resolveWebSessionForAccount()` 仍未在 `mafile_content` 破损时回退 `TokenStore`；2) [node_sidecar/src/inventoryService.js](/C:/Users/18220/Desktop/cs2_alchemy/node_sidecar/src/inventoryService.js) 仍绕开共享 `steamHttpClient`；3) [node_sidecar/src/steamAccountTools.js](/C:/Users/18220/Desktop/cs2_alchemy/node_sidecar/src/steamAccountTools.js) 的 `fetchTradeUrl()` 仍缺上游 `steamcommunity.getTradeURL()` 首选路径。不要先扩功能再拖着这些底层偏差不管。
- 2026-04-25: 社区 `Web Inventory` 拉取链现在有独立 CMD 证据流：`/api/accounts/:username/inventory` 与 `/api/accounts/batch-inventory` 会输出 `web_inventory_fetch` 日志，包含 `step=start/session/done/failed`、每页 `request/response/error`、脱敏 `sessionid`/cookie 摘要、HTTP 状态码与返回体片段。后续若魔尊说“WEB 拉库存报错但信息不明确”，先看 CMD 里的 `web_inventory_fetch`，不要只盯前端弹窗报错。
- 2026-04-25: `Steam Guard` 绑定令牌纠正方案必须坚持两条后端分支：无旧令牌时走 `new_enroll`（`enableTwoFactor/finalizeTwoFactor`），已有旧令牌时走 `replace_existing`（`RemoveAuthenticatorViaChallengeStart/Continue`），不能再把 `status=29` 当成终态错误直接返回。
- 2026-04-25: `Steam Guard` 绑定成功后生成的 `maFile` 必须匹配本项目当前真实消费契约，至少包含 `Session.SteamID`、`Session.SteamLoginSecure`、顶层 `access_token`、`identity_secret`、`fully_enrolled`；禁止再次写出 `Session: {}` 这种会把 Web Session 链路直接打坏的残缺结构。
- 2026-04-25: `Steam Guard` 令牌详情弹窗不能再把“本地 decrypt / TOTP 重算失败”升级成整窗 `ERROR`。前端必须先显示服务端返回的 `currentTotp` 作为兜底，再尝试本地重算；同时 `/api/accounts/token-detail` 返回的 `steamData` 必须保留 parser fallback 所需的 `shared_secret`，并继续兼容 `shared_secret / response.shared_secret / SharedSecret / Response.SharedSecret` 这些历史结构入口。
- 2026-04-25: `/api/craft/assist-select` 当前活的前端出站点只剩 [node_sidecar/ui/app.js](/C:/Users/18220/Desktop/cs2_alchemy/node_sidecar/ui/app.js) 两处：单账号自动选材与 batch helper。两者都发送 `username / target_wear / wear_approach_mode / materials / use_component_items / blocked_ids / include_cooling / wear_offset_pct / enable_fast_craft_assist`；顶层 `wear_filter_mode / selected_item_ids / include_component_items` 只应视作 `uiServer` 兼容层或旧数据升格入口，本地 `batch_craft_ui_prefs_v1.fast_mode / approach_mode` 与 `materials[].items[].wear_filter_mode` 不属于网络旧契约。另一个容易混淆的现场点是：调用侧响应仍兼容 `item_ids || itemIds`，因此若后续继续做“旧契约清理”，要把响应别名一起纳入审计，而不是只盯请求体。
- 2026-04-16: `craftAssistService` 的成功返回路径带有终局磨损校验。该硬闸只作用于 `below` 模式，`infinite` 模式必须跳过，避免误伤“逼近磨损”选择。若终局校验拦截，失败结果与服务端日志都要带上 `safe_target`、`approach_mode`、`item_ids`，并展开 `selected_items` 明细，其中至少包含每件材料的 `asset_id`、`name`、`absolute_wear`、`relative_wear`。
- 2026-04-19: 仓库根目录桌面入口语义已定版。`main_ui_node_desktop.js` / `run.bat` 属于默认正式入口，默认走 `prod_login`；开发直通必须通过显式入口 `run-dev.bat` 或 `scripts/start-client-dev.ps1` 进入，避免把 `dev_auto_bundle` 混入主启动链。
- 2026-04-25: 上述“正式入口默认 `prod_login`”规则不能覆盖 Node inspector 调试态。若 [main_ui_node_desktop.js](/C:/Users/18220/Desktop/cs2_alchemy/main_ui_node_desktop.js) 是在 `--inspect` / `--inspect-brk` 或 `NODE_OPTIONS` 含 inspect 标记的情况下启动，launcher 必须自动回到 `dev_auto_bundle`，否则本地调试会被 guest/login gate 截断，看起来像“本地账号没加载”，而实际根目录 `csgo_skins.db` / `accounts.json` 数据仍在。
- 2026-04-25: `run-dev` 下若服务端 `/api/client-auth/state` 与 `/api/accounts` 都正常，但页面仍显示“尚未保存账号”或停在空壳，先查 [node_sidecar/ui/app.js](/C:/Users/18220/Desktop/cs2_alchemy/node_sidecar/ui/app.js) 启动链是否在 `loadLicenseState()` 前调用了不存在的 helper。此次真实根因是 `init()` 里残留 `initWebInvBindings()`，浏览器直接抛 `ReferenceError`，导致账号加载根本没开始；这类已删 helper 的启动引用必须有静态回归测试兜底。
- 2026-04-25: Steam Market 上架链路里的 ID 语义已经核实：`/market/sellitem/` 用 `assetid` 指向待上架库存物品；手机确认列表里的 `id` 是确认单 ID；`creator_id`（在 `steamcommunity` 里暴露为 `CConfirmation.creator`）才是被确认对象 ID，并被 `acceptConfirmationForObject(objectID)` 直接拿来匹配 trade offer / market listing。后续若继续做“上架后手动确认回指原物品”，不要再把 `creator_id` 混叫成 `itemid`。
- 2026-04-25: 根目录 [csgo_skins.db](/C:/Users/18220/Desktop/cs2_alchemy/csgo_skins.db) 现在视为本机运行态数据库，不再作为仓库受控资产。它必须保持 `.gitignore` 忽略且从 Git 索引中摘除，避免本地账号/库存运行态把仓库持续污染；若后续打包或首启引导仍需要“种子 DB”，必须改用单独的稳定资源文件，不能再回退为直接依赖这份运行态 DB。
- 2026-04-25: Windows 打包链现在应从 [node_sidecar/build/csgo_skins.seed.db](/C:/Users/18220/Desktop/cs2_alchemy/node_sidecar/build/csgo_skins.seed.db) 提供数据库种子，并在 [node_sidecar/electron-builder.yml](/C:/Users/18220/Desktop/cs2_alchemy/node_sidecar/electron-builder.yml) 中以 `build/csgo_skins.seed.db -> csgo_skins.db` 进入安装包。禁止再次把 `../csgo_skins.db` 作为 `extraResources` 来源，否则干净克隆或 CI 会重新依赖本机运行态文件。
- 2026-04-20: 仓库根目录只保留桌面入口，不再保留任何 CLI 兼容入口。用户正式入口固定为 `run.bat` / `main_ui_node_desktop.js`，开发入口固定为 `run-dev.bat`；如需命令行链路，必须直接使用 `node node_sidecar/src/main.js ...` 或 `node_sidecar` 内的 `npm run ...`。
- 2026-04-19: Windows packaged 客户端必须自带三份首启种子资源：`client_config.json`、`schema_cache.json`、`csgo_skins.db`。安装包首次启动时要把它们从 `resources/` 复制到 Electron `userData`，避免用户安装后因远端控制台地址或基础数据缺失而不可用。
- 2026-04-19: Windows packaged 运行时不得再直接依赖仓库根目录 `tools/`。凡是 packaged 模式需要的逻辑，必须落在 `node_sidecar/src/**` 或已纳入 `extraResources` 的稳定资源里，否则安装后会在模块解析阶段直接崩溃。
- 2026-04-19: 这台 Windows 机器上，`electron-builder` 的内置 `signAndEditExecutable` 会走 `app-builder.exe rcedit`，实际仍回落到旧版 `winCodeSign-2.6.0` 下载链，容易卡在 symlink 权限或网络超时。当前项目若只为保证安装包可构建与用户可见图标正确，应优先采用“`signAndEditExecutable: false` + packaged `app-icon.ico` + `build/installer.nsh` 重建快捷方式图标 + `BrowserWindow.icon`”的组合，而不是继续依赖内置 EXE 资源编辑。
