# 代理配置指南

## 问题：为什么 Steam 客户端可以登录，但程序无法登录？

**原因**：Steam 官方客户端使用系统代理或内置代理支持，而你的程序使用 `gevent` socket，不会自动使用系统代理。

## 解决方案

### 方案 1：使用代理配置向导（推荐）

**最简单的方法**，运行配置向导：

```bash
python setup_proxy.py
```

按照提示配置你的代理：
1. 选择代理类型（SOCKS5/SOCKS4/HTTP）
2. 输入代理地址和端口
3. 如需认证，输入用户名和密码
4. 测试代理连接

### 方案 2：手动编辑配置文件

编辑 `config.py` 文件：

```python
# SOCKS5 代理（Clash/V2Ray/SSR 默认）
PROXY_URL = "socks5://127.0.0.1:1080"
USE_PROXY = True
```

**常见代理配置**：

| 代理工具 | 默认配置 |
|---------|---------|
| Clash | `socks5://127.0.0.1:7891` |
| V2Ray | `socks5://127.0.0.1:1080` |
| SSR | `socks5://127.0.0.1:1080` |
| Shadowsocks | `socks5://127.0.0.1:1080` |

### 方案 3：使用 Proxifier（无需修改代码）

如果你不想修改配置，可以使用 Proxifier：

1. 下载安装 Proxifier
2. 配置代理服务器
3. 添加规则：`python.exe` → 通过代理
4. 运行程序（使用原配置）

### 方案 4：使用 Clash TUN 模式（无需修改代码）

如果你使用 Clash：

1. 打开 Clash
2. 设置 → TUN 模式 → 开启
3. 运行程序（使用原配置）

## 配置示例

### SOCKS5 代理（无认证）
```python
PROXY_URL = "socks5://127.0.0.1:1080"
USE_PROXY = True
```

### SOCKS5 代理（带认证）
```python
PROXY_URL = "socks5://username:password@127.0.0.1:1080"
USE_PROXY = True
```

### HTTP 代理
```python
PROXY_URL = "http://127.0.0.1:8080"
USE_PROXY = True
```

### 不使用代理（直连）
```python
PROXY_URL = None
USE_PROXY = False
```

## 测试代理

配置完成后，测试代理是否正常：

```bash
python setup_proxy.py
# 选择 "2. 测试代理连接"
```

或者运行网络诊断：

```bash
python network_test.py
```

## 常见问题

### Q1: 配置了代理但仍然连接失败

**检查清单**：
1. 代理服务器是否正在运行？
2. 代理地址和端口是否正确？
3. 是否需要认证但未配置？
4. 防火墙是否阻止了连接？

**解决方法**：
```bash
# 测试代理连接
python setup_proxy.py
# 选择 "2. 测试代理连接"
```

### Q2: 如何查看我的代理配置？

**Clash**：
- 打开 Clash → 设置 → 端口
- 通常是 `127.0.0.1:7891` (SOCKS5)

**V2Ray**：
- 查看 V2Ray 配置文件
- 通常是 `127.0.0.1:1080` (SOCKS5)

### Q3: 代理需要认证怎么办？

编辑 `config.py`：
```python
PROXY_URL = "socks5://username:password@127.0.0.1:1080"
USE_PROXY = True
```

### Q4: 我在国外，不需要代理

编辑 `config.py`：
```python
PROXY_URL = None
USE_PROXY = False
```

或者直接删除 `config.py` 文件。

## 验证配置

配置完成后，运行主程序：

```bash
python main.py
```

如果看到以下日志，说明代理配置成功：

```
auth INFO 使用代理模式连接
ws_connection_proxy INFO 使用代理: 127.0.0.1:1080 (类型: SOCKS5)
ws_connection_proxy INFO 正在连接 WebSocket CM: 162.254.197.42:443
ws_connection_proxy DEBUG 步骤 1/4: 通过代理建立 TCP 连接...
ws_connection_proxy DEBUG TCP 连接成功，本地地址: 127.0.0.1
ws_connection_proxy INFO ✓ WebSocket 连接成功: 162.254.197.42:443
```

## 技术说明

### 为什么需要代理支持？

1. **gevent socket 限制**：`gevent` 的 socket 不支持 HTTP_PROXY 环境变量
2. **Steam CM 服务器**：位于国外，中国大陆直连可能不稳定
3. **WebSocket 协议**：需要在 TCP 层面代理，不能用简单的 HTTP 代理

### 实现原理

使用 `PySocks` 库在 socket 层面实现代理：

```python
# 创建 SOCKS socket
sock = socks.socksocket(socket.AF_INET, socket.SOCK_STREAM)
sock.set_proxy(
    proxy_type=socks.SOCKS5,
    addr="127.0.0.1",
    port=1080
)
sock.connect(("162.254.197.42", 443))
```

### 支持的代理类型

- ✅ SOCKS5（推荐）
- ✅ SOCKS4
- ✅ HTTP
- ✅ 带认证的代理

## 快速开始

**最快的配置方法**：

```bash
# 1. 运行配置向导
python setup_proxy.py

# 2. 选择 "1. 配置代理"

# 3. 按提示输入：
#    - 代理类型：1 (SOCKS5)
#    - 地址：127.0.0.1
#    - 端口：1080 (或你的代理端口)
#    - 认证：n (如果不需要)

# 4. 测试连接
python setup_proxy.py
# 选择 "2. 测试代理连接"

# 5. 运行主程序
python main.py
```

完成！现在你的程序应该可以通过代理连接 Steam 了。
