# CS2 炼金术工具 - 认证功能说明

## 功能概述

已成功实现基于 Steam IAuthenticationService 的现代化认证机制，支持：
- ✅ 使用 refresh_token 自动登录
- ✅ 断联后自动重连
- ✅ 长期有效（200天）
- ✅ 无需每次输入 TOTP

## 核心实现

### 1. 首次登录（获取 refresh_token）

```python
from auth import login_with_totp

# 首次登录需要输入 TOTP
client = login_with_totp(username, password, totp_code)
# refresh_token 会自动保存到 login_keys.json
```

**流程**：
1. 通过 HTTPS API 调用 IAuthenticationService
2. 获取 refresh_token（200天有效）和 access_token（24小时有效）
3. 保存 refresh_token 到本地
4. 使用 access_token 登录 CM 服务器

### 2. 后续登录（使用 refresh_token）

```python
from auth import login

# 自动使用保存的 refresh_token 登录
client = login(username, password)
# 无需输入 TOTP，直接登录成功
```

**流程**：
1. 从 login_keys.json 读取 refresh_token
2. 连接 CM 服务器
3. **等待加密通道建立**（关键步骤）
4. 使用 refresh_token 直接登录 CM
5. 登录成功

### 3. 断联重连

```python
# 第一次连接
client = login(username, password)
# ... 使用一段时间后断开 ...
client.disconnect()

# 重新连接（使用相同的 refresh_token）
client = login(username, password)
# 成功重连
```

## 技术细节

### Token 结构

#### refresh_token（长期凭证）
```json
{
  "aud": ["client", "web", "renew", "derive"],
  "exp": 1790720634,  // 约200天有效期
  "iss": "steam",
  "sub": "76561198815742059"
}
```

#### access_token（短期凭证）
```json
{
  "aud": ["client", "web"],
  "exp": 1772521266,  // 约24小时有效期
  "iss": "r:0010_27C7D693_DD2BA",
  "rt_exp": 1790720634  // 包含 refresh_token 的过期时间
}
```

### 关键发现

1. **refresh_token 可以直接用于 CM 登录**
   - refresh_token 的 `aud` 包含 `"client"`，满足 CM 登录要求
   - 无需每次刷新 access_token
   - 简化了登录流程

2. **必须等待加密通道建立**
   - 在发送 ClientLogon 之前，必须等待 `channel_secured` 事件
   - 否则会在加密握手过程中断开连接

3. **access_token 不能作为 refresh_token 使用**
   - access_token 缺少 `"renew"` 和 `"derive"` 受众
   - 有效期短（24小时 vs 200天）
   - 用途不同（短期认证 vs 长期存储）

## 文件说明

### auth.py
核心认证模块，包含：
- `login_with_totp()` - 首次登录
- `login()` - 自动登录
- `_get_tokens_via_totp()` - 通过 HTTPS API 获取 tokens
- `_login_with_access_token()` - 使用 token 登录 CM
- Token 缓存管理函数

### login_keys.json
存储 refresh_token 的文件：
```json
{
  "username": "eyAidHlwIjogIkpXVCIsICJhbGciOiAiRWREU0EiIH0..."
}
```

### 测试脚本

#### test_login_方案A.py
测试首次登录和自动登录：
```bash
# 首次登录
python test_login_方案A.py --init

# 自动登录
python test_login_方案A.py
```

#### test_reconnect.py
测试断联重连：
```bash
python test_reconnect.py
```

## 使用示例

### 主程序集成

```python
from auth import login, login_with_totp
from client import CS2Client

# 方式1：首次登录
try:
    steam_client = login(username, password)
except ValueError as e:
    # 没有 refresh_token，需要首次登录
    totp = input("请输入手机令牌验证码: ")
    steam_client = login_with_totp(username, password, totp)

# 创建 CS2 客户端
cs2 = CS2Client(steam_client)
cs2.launch()

# ... 使用 CS2 客户端 ...

# 断开连接
steam_client.disconnect()

# 重新连接
steam_client = login(username, password)
cs2 = CS2Client(steam_client)
cs2.launch()
```

### 错误处理

```python
from steam.enums import EResult

try:
    client = login(username, password)
except ValueError as e:
    if "未找到 refresh_token" in str(e):
        # 需要首次登录
        print("请先完成首次登录")
    elif "登录失败" in str(e):
        # refresh_token 可能已过期
        print("Token 已过期，请重新登录")
        # 清除旧 token 并重新登录
        totp = input("请输入手机令牌验证码: ")
        client = login_with_totp(username, password, totp)
```

## 测试结果

### ✅ 首次登录测试
```
=== 首次登录（获取 refresh_token）===
✓ 首次登录成功！
  用户名: 1822049852
  Steam ID: 76561198815742059
  已登录: True
refresh_token 已保存到 login_keys.json
```

### ✅ 自动登录测试
```
=== 自动登录（使用 refresh_token）===
✓ 自动登录成功！
  用户名: 1822049852
  Steam ID: 76561198815742059
  已登录: True
保持连接 10 秒测试稳定性...
  连接状态: True
  登录状态: True
```

### ✅ 断联重连测试
```
第一次连接：使用 refresh_token 登录
[OK] 第一次登录成功！
主动断开连接...
[OK] 已断开

第二次连接：断联后重连
[OK] 重连成功！
保持连接 10 秒测试稳定性...
  连接状态: True
  登录状态: True
[OK] 断联重连测试完成
```

## 注意事项

1. **Token 安全**
   - login_keys.json 包含敏感信息，不要提交到版本控制
   - 建议添加到 .gitignore

2. **Token 有效期**
   - refresh_token 有效期约 200 天
   - 过期后需要重新输入 TOTP
   - 可以通过解析 JWT 的 `exp` 字段检查有效期

3. **网络稳定性**
   - 加密通道建立超时设置为 10 秒
   - 如果网络不稳定，可能需要调整超时时间

4. **多账号支持**
   - login_keys.json 支持存储多个账号的 token
   - 每个账号的 token 独立管理

## 后续优化建议

1. **Token 有效期检查**
   - 添加检查 token 有效期的功能
   - 在 token 快过期时提前提醒用户

2. **自动重连机制**
   - 检测连接断开
   - 自动调用 login() 重连

3. **错误重试**
   - 网络错误时自动重试
   - 指数退避策略

4. **日志优化**
   - 生产环境降低日志级别
   - 敏感信息脱敏

## 参考资料

- [node-steam-session](https://github.com/DoctorMcKay/node-steam-session) - Steam 认证机制参考
- [ValvePython/steam](https://github.com/ValvePython/steam) - Python Steam 客户端库
- Steam IAuthenticationService API 文档
